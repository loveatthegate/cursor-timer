# cursor-timer

独立运行的倒计时工具。

## 使用

用浏览器直接打开：

```bash
open timer.html
```

或双击 `timer.html`。

## 功能

- 自定义倒计时时长（时 / 分 / 秒），支持快捷预设
- 时间到后语音提示、音乐提示，或两者同时
- 提示持续时长可自定义（默认 10 秒后自动停止）
- 可调音量、内置音乐风格、自定义音频文件、语音文案 / 语言 / 语速
- 设置自动保存在本机浏览器
